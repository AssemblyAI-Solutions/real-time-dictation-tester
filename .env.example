# AssemblyAI API key — https://www.assemblyai.com/dashboard
# Copy this file to .env.local and fill in your key. The key is only ever read
# server-side; the browser receives a short-lived token instead.
ASSEMBLYAI_API_KEY=
